apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-token
  namespace: cloudflared
type: Opaque
stringData:
  # Cloudflare Zero Trust 대시보드에서 터널 생성 후 발급받은 토큰
  # https://one.dash.cloudflare.com > Networks > Tunnels > Create a tunnel
  token: "your-token-here"