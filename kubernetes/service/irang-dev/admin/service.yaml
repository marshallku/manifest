apiVersion: v1
kind: Service
metadata:
  name: irang-admin-web
  namespace: irang-dev
  labels:
    app: irang-admin-web
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30507
  selector:
    app: irang-admin-web
