apiVersion: v1
kind: Service
metadata:
  name: irang-web
  namespace: irang-dev
  labels:
    app: irang-web
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30509
  selector:
    app: irang-web
