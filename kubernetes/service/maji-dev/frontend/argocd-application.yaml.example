# Register maji-web-dev with ArgoCD (run once, manually — Application CRs
# are not synced from this repo). `.example` so the Application that watches
# this directory does not try to apply it (it ignores *.yaml.example).
#
#   kubectl apply -f kubernetes/service/maji-dev/frontend/argocd-application.yaml.example
#
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: maji-web-dev
  namespace: argocd
spec:
  destination:
    namespace: maji-dev
    server: https://kubernetes.default.svc
  project: miniapp
  source:
    path: kubernetes/service/maji-dev/frontend
    repoURL: https://github.com/marshallku/manifest.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
